interface:
  display_name: Update Dependencies
  short_description: Update deps and required config coverage.
  default_prompt: Use $update-dependencies to update one package and handle required follow-up changes.
